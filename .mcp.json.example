{
  "mcpServers": {
    "example-api": {
      "type": "http",
      "url": "${EXAMPLE_MCP_URL:-https://api.example.com}/mcp",
      "headers": {
        "Authorization": "Bearer ${EXAMPLE_MCP_TOKEN}"
      }
    }
  }
}